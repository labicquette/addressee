[project]
name = "addressee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lightning>=2.6.0",
    "pandas>=2.3.3",
    "seaborn>=0.13.2",
    "soundfile>=0.13.1",
    "spidr>=0.1.3",
    "torch==2.8.0",
    "torchaudio==2.8.0",
    "wandb>=0.23.1",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

